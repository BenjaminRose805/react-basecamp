{
  "_note": "Template for machine-readable spec metadata. Fill {{placeholders}} with actual values. For nested specs: parent_project='basecamp', name='authentication'. For standalone: omit parent_project and parent_feature.",
  "_note_linear": "Optional: Remove linear block if Linear is not configured",
  "name": "{{feature}}",
  "parent_project": "{{parent_project}}",
  "parent_feature": "{{parent_feature}}",
  "status": "{{status}}",
  "created": "{{date}}",
  "updated": "{{date}}",
  "author": "{{agent}}",
  "version": "{{semver}}",
  "files": {
    "requirements": "requirements.md",
    "design": "design.md",
    "tasks": "tasks.md",
    "summary": "summary.md",
    "meta": "meta.yaml"
  },
  "phases": ["{{phase_1}}", "{{phase_2}}", "{{phase_3}}"],
  "tasks": [
    {
      "id": "{{task_id}}",
      "title": "{{task_title}}",
      "status": "{{task_status}}",
      "assignee": "{{task_assignee}}"
    }
  ],
  "linear": {
    "identifier": "{{linear_id}}",
    "url": "{{linear_url}}"
  }
}
