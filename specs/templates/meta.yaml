# Shared metadata for feature specification
# Source of truth for spec_id, feature name, status, and timestamps

spec_id: "{{id}}"
feature: "{{name}}"
status: "{{status}}"
created: "{{created}}"
updated: "{{updated}}"
author: "{{agent}}"
version: "{{semver}}"
