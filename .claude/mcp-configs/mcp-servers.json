{
  "mcpServers": {
    "_comment": "=== REQUIRED FOR REACT-BASECAMP ===",

    "cclsp": {
      "command": "npx",
      "args": ["-y", "cclsp"],
      "description": "TypeScript LSP - code intelligence, go-to-definition, find references",
      "priority": "required"
    },

    "next-devtools": {
      "command": "npx",
      "args": ["-y", "next-devtools-mcp@latest"],
      "description": "Next.js DevTools - route info, build status, server actions",
      "priority": "required"
    },

    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest"],
      "description": "Browser automation - E2E testing, screenshots, page interactions",
      "priority": "required"
    },

    "vitest": {
      "command": "npx",
      "args": ["-y", "@djankies/vitest-mcp"],
      "description": "Test runner - run tests, coverage, watch mode",
      "priority": "required"
    },

    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      },
      "description": "GitHub operations - PRs, issues, repos, code search",
      "priority": "required"
    },

    "_comment2": "=== RECOMMENDED ===",

    "context7": {
      "command": "npx",
      "args": ["-y", "@context7/mcp-server"],
      "description": "Live documentation lookup - prevents hallucinated APIs",
      "priority": "recommended"
    },

    "spec-workflow": {
      "command": "npx",
      "args": ["-y", "@pimzino/spec-workflow-mcp@latest", "."],
      "description": "SDD workflow - spec management, dashboard",
      "priority": "recommended"
    },

    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"],
      "description": "Persistent memory - remember context across sessions",
      "priority": "recommended"
    },

    "sentry": {
      "type": "http",
      "url": "https://mcp.sentry.dev/mcp",
      "description": "Production error monitoring - issues, stack traces, Seer AI analysis",
      "priority": "recommended"
    },

    "figma": {
      "type": "http",
      "url": "https://mcp.figma.com/mcp",
      "description": "Design context - frames, tokens, components from Figma files",
      "priority": "recommended"
    },

    "linear": {
      "command": "npx",
      "args": ["-y", "@anthropic/linear-mcp@latest"],
      "description": "Issue tracking - create/update issues, track work, link PRs",
      "priority": "recommended"
    },

    "_comment3": "=== OPTIONAL ===",

    "sequential-thinking": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"],
      "description": "Chain-of-thought reasoning for complex problems",
      "priority": "optional"
    },

    "vercel": {
      "type": "http",
      "url": "https://mcp.vercel.com",
      "description": "Vercel deployments and projects",
      "priority": "optional"
    },

    "firecrawl": {
      "command": "npx",
      "args": ["-y", "firecrawl-mcp"],
      "env": {
        "FIRECRAWL_API_KEY": "${FIRECRAWL_API_KEY}"
      },
      "description": "Web scraping and documentation fetching",
      "priority": "optional"
    }
  },

  "setup": {
    "instructions": [
      "1. Copy the servers you need to your ~/.claude.json mcpServers section",
      "2. Replace ${VAR} placeholders with actual values or set environment variables",
      "3. Required servers should be enabled for full react-basecamp functionality",
      "4. Keep under 10 MCPs enabled to preserve context window"
    ],
    "quick_setup": [
      "claude mcp add cclsp -- npx cclsp",
      "claude mcp add next-devtools -- npx -y next-devtools-mcp@latest",
      "claude mcp add playwright -- npx @playwright/mcp@latest",
      "claude mcp add vitest -- npx @djankies/vitest-mcp",
      "claude mcp add github -- npx @modelcontextprotocol/server-github",
      "claude mcp add context7 -- npx @context7/mcp-server",
      "claude mcp add spec-workflow -- npx -y @pimzino/spec-workflow-mcp@latest .",
      "claude mcp add --transport http sentry https://mcp.sentry.dev/mcp",
      "claude mcp add --transport http figma https://mcp.figma.com/mcp",
      "claude mcp add linear -- npx @anthropic/linear-mcp@latest"
    ]
  },

  "project_overrides": {
    "description": "Add these to .claude.json in project root to disable per-project",
    "example": {
      "disabledMcpServers": ["firecrawl", "vercel"]
    }
  }
}
